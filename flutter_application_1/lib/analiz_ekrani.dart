import 'package:flutter/material.dart';

class AnalizEkrani extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Analiz")),
      body: Center(child: Text("Analiz Ekranı")),
    );
  }
}
